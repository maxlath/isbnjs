#!/usr/bin/env node
const [ isbn ] = process.argv.slice(2)

if (!isbn || isbn.length === 0) {
  console.error('expect an isbn as unique argument')
  process.exit(1)
}

const isbnjs = require('../isbn.js')
console.log(isbnjs.parse(isbn))
